<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Members Only</title>
</head>
<body>
    <%- include("nav") %>
    <h1><%= title %></h1>
    <p>User: <%= user.email %></p>
    <a href="/login">Log In</a>
    <% if (locals.subpage && locals.subargs) {%>
        <%- include(subpage, subargs) %>
    <% } %>
    <%- include("footer") %>
</body>
</html>